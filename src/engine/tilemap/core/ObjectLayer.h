#pragma once

// 这个头文件只是为了完整性，主要内容已在 TileLayer.h 中定义
// 包含对象层相关的前向声明和类型定义

#include "TileLayer.h"

namespace PrismaEngine {

// ObjectLayer 和 MapObject 已在 TileLayer.h 中定义
// 此文件提供便捷的 include 路径

} // namespace PrismaEngine
