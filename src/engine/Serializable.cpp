#include "Serializable.h"
#include "Export.h"
#include <filesystem>

namespace Prisma {
    namespace Serialization {
        // Implementations for methods that might be needed in cpp if not inline
    }
}
