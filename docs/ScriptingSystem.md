# 脚本系统设计

> **状态**: 🔲 规划中
> **优先级**: 中
> **依赖**: 资源管理系统完成

## 概述

PrismaEngine 的脚本系统将允许使用 C# 编写游戏逻辑，通过 .NET 运行时与引擎核心进行交互。

## 设计目标

- 支持 C# 作为主要脚本语言
- 热重载 - 运行时重新加载脚本
- 与引擎核心类型互操作（GameObject, Component, Transform 等）
- 性能优化 - 脚本调用开销最小化
- 调试支持 - 断点、日志、错误追踪

## 技术选型

| 组件 | 技术方案 | 状态 |
|------|----------|------|
| 运行时 | .NET 8+ / Mono | 待定 |
| 语言 | C# 11 / C# 12 | 待定 |
| 互操作 | C++/CLI / CSharp for Unity | 待定 |
| 编译 | Roslyn / .NET Compiler Platform | 待定 |

## 架构设计

```
┌─────────────────────────────────────────────────┐
│                   Game Layer                  │
│  (C# Scripts - MonoBehaviour, GameLogic)      │
├─────────────────────────────────────────────────┤
│              Scripting API Layer              │
│  (GameObject, Component, Transform, ...)      │
├─────────────────────────────────────────────────┤
│              Script Binding Layer             │
│  (C++/CLI bindings, Mono runtime)             │
├─────────────────────────────────────────────────┤
│                  Engine Core                   │
│  (Renderer, Physics, Audio, Resource, ...)     │
└─────────────────────────────────────────────────┘
```

## API 设计示例

```csharp
// 游戏脚本示例
public class PlayerController : MonoBehaviour
{
    public float moveSpeed = 5.0f;

    void Update()
    {
        float horizontal = Input.GetAxis("Horizontal");
        float vertical = Input.GetAxis("Vertical");

        Vector3 movement = new Vector3(horizontal, 0, vertical) * moveSpeed * Time.deltaTime;
        transform.position += movement;
    }
}
```

## 开发计划

| 阶段 | 内容 | 状态 |
|------|------|------|
| Phase 1 | 运行时集成 (Mono / .NET) | ⏳ 计划中 |
| Phase 2 | 核心类型绑定 | ⏳ 计划中 |
| Phase 3 | 脚本加载与执行 | ⏳ 计划中 |
| Phase 4 | 热重载支持 | ⏳ 计划中 |
| Phase 5 | 性能优化 | ⏳ 计划中 |

## 参考资料

- [Unity Scripting](https://docs.unity3d.com/ScriptReference/)
- [Mono Embedding](https://www.mono-project.com/docs/embedding/)
- [.NET Host](https://learn.microsoft.com/en-us/dotnet/core/tutorials/nethost)

---

*文档创建时间: 2025-12-25*
