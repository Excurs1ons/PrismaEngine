name: Build Windows Launcher

on:
  push:
    branches: [ main, develop ]
    paths:
      - 'src/launcher/windows/**'
      - '.github/workflows/build-windows-launcher.yml'
  pull_request:
    branches: [ main ]
    paths:
      - 'src/launcher/windows/**'
      - '.github/workflows/build-windows-launcher.yml'
  workflow_dispatch:
    inputs:
      build_type:
        description: 'Build type'
        required: true
        default: 'debug'
        type: choice
        options:
        - debug
        - release

env:
  CMAKE_VERSION: '3.31.0'

jobs:
  build-windows-launcher:
    name: Windows Launcher
    runs-on: windows-latest

    env:
      BUILD_TYPE: ${{ github.event.inputs.build_type || 'debug' }}

    steps:
    - name: Checkout
      uses: actions/checkout@v4
      with:
        submodules: false

    - name: Setup CMake
      uses: jwlawson/actions-setup-cmake@v2.0
      with:
        cmake-version: ${{ env.CMAKE_VERSION }}

    - name: Cache CMake build
      uses: actions/cache@v4
      with:
        path: ./build/launcher-windows-x64-${{ env.BUILD_TYPE }}
        key: ${{ runner.os }}-launcher-${{ env.BUILD_TYPE }}-${{ hashFiles('src/**', 'cmake/**', 'CMakeLists.txt', 'CMakePresets.json') }}

    - name: Clean old dependencies cache
      shell: pwsh
      run: Remove-Item -Path "build\launcher-windows-x64-${{ env.BUILD_TYPE }}\_deps\stb" -Recurse -Force -ErrorAction SilentlyContinue

    - name: Configure
      run: cmake --preset launcher-windows-x64-${{ env.BUILD_TYPE }}

    - name: Build Launcher
      run: cmake --build build/launcher-windows-x64-${{ env.BUILD_TYPE }} --config ${{ env.BUILD_TYPE == 'debug' && 'Debug' || 'Release' }} --target Launcher --parallel

    - name: Build Game
      run: cmake --build build/launcher-windows-x64-${{ env.BUILD_TYPE }} --config ${{ env.BUILD_TYPE == 'debug' && 'Debug' || 'Release' }} --target Game --parallel

    - name: Package
      run: |
        mkdir -p package-x64-${{ env.BUILD_TYPE }}
        $cfg = "${{ env.BUILD_TYPE == 'debug' && 'Debug' || 'Release' }}"
        copy "build\launcher-windows-x64-${{ env.BUILD_TYPE }}\bin\$cfg\PrismaLauncher.exe" "package-x64-${{ env.BUILD_TYPE }}\"
        copy "build\launcher-windows-x64-${{ env.BUILD_TYPE }}\bin\$cfg\*.dll" "package-x64-${{ env.BUILD_TYPE }}\" 2>$null || echo No DLLs
        xcopy assets "package-x64-${{ env.BUILD_TYPE }}\assets" /E /I /Y 2>$null || echo No assets
        xcopy projects "package-x64-${{ env.BUILD_TYPE }}\projects" /E /I /Y 2>$null || echo No projects
        cd package-x64-${{ env.BUILD_TYPE }}
        7z a ../PrismaGame-x64-${{ env.BUILD_TYPE }}.zip *

    - name: Upload artifacts
      uses: actions/upload-artifact@v4
      with:
        name: PrismaGame-x64-${{ env.BUILD_TYPE }}
        path: PrismaGame-x64-${{ env.BUILD_TYPE }}.zip
        retention-days: 30
